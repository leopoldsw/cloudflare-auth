# @cf-auth/client

Browser client SDK for Cloudflare Auth. It calls same-origin auth endpoints,
sends credentials by default, forwards optional Turnstile tokens on auth
mutations, supports custom token consume pages, and exposes typed client errors.

Cloudflare Auth is an independent open-source project and is not affiliated with, endorsed by, or sponsored by Cloudflare.
